const assert = require('assert');
const waiterLogin = require('../controllers/waiterLoginController');

describe('All tests for my function', function(done) {

  it('should login the right user', function() {
    assert.equals();
  });

});
