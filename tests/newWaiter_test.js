const assert = require('assert');
const newWaiter = require('../models/waiterSchema');

describe('Waiter Availability tests', function(done) {

  it('Should add the new waiter to the database', function() {
    assert()
  });

});
